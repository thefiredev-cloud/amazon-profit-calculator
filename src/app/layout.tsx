import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Amazon Profit Calculator - See Your Real FBA Profit",
  description: "Calculate your real Amazon FBA profit with accurate fee calculations. Stop guessing if your products are profitable. Get instant profit breakdown with all Amazon fees included.",
  keywords: "Amazon FBA calculator, Amazon profit calculator, FBA fee calculator, Amazon seller tools",
  authors: [{ name: "Amazon Profit Calculator" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
